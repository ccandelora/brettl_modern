<!DOCTYPE html>
<html>
  <head>
    <title>Bunk List - <%= yield(:title) %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <style>
      /* Print and screen styles */
      body {
        margin: 0;
        padding: 0.5in;
        font-family: Arial, sans-serif;
        font-size: 11pt;
        line-height: 1.3;
        background: white;
        color: black;
      }

      /* Header styling */
      .header {
        text-align: center;
        margin-bottom: 20px;
        padding: 10px;
        border-bottom: 2px solid #000;
      }

      .header h1 {
        font-size: 18pt;
        margin: 0;
        color: #000;
      }

      .header h2 {
        font-size: 14pt;
        margin: 5px 0;
        color: #333;
      }

      .header p {
        font-size: 11pt;
        margin: 5px 0;
        font-style: italic;
      }

      /* Section titles */
      .section-title {
        background-color: #f0f0f0;
        padding: 5px 10px;
        margin: 15px 0 10px 0;
        font-weight: bold;
        font-size: 12pt;
        page-break-after: avoid;
      }

      /* Room titles */
      .room-title {
        font-weight: bold;
        margin: 10px 0 5px 0;
        font-size: 11pt;
        page-break-after: avoid;
      }

      /* Table styling */
      table {
        width: 100%;
        border-collapse: collapse;
        margin-bottom: 15px;
        page-break-inside: avoid;
      }

      th, td {
        border: 1px solid #000;
        padding: 4px 6px;
        text-align: left;
        font-size: 10pt;
      }

      th {
        background-color: #f0f0f0;
        font-weight: bold;
      }

      tr:nth-child(even) {
        background-color: #f9f9f9;
      }

      /* Column widths */
      th:nth-child(1), td:nth-child(1) { width: 15%; }
      th:nth-child(2), td:nth-child(2) { width: 35%; }
      th:nth-child(3), td:nth-child(3) { width: 35%; }
      th:nth-child(4), td:nth-child(4) { width: 15%; }

      /* Statistics Table */
      .statistics-table {
        width: 100%;
        margin-bottom: 20px;
        page-break-inside: avoid;
      }

      .statistics-table th,
      .statistics-table td {
        text-align: center;
        padding: 6px;
      }

      .statistics-table td:first-child {
        text-align: left;
      }

      .statistics-table tr.totals {
        border-top: 2px solid #000;
        font-weight: bold;
      }

      .statistics-table tr.totals td {
        background-color: #f0f0f0;
      }

      /* Print specific */
      @page {
        size: letter portrait;
        margin: 0.5in;
      }

      @media print {
        body { padding: 0; }
        .no-print { display: none !important; }
        .page-break { page-break-after: always; }
      }
    </style>
  </head>
  <body class="bg-white">
    <div class="no-print mb-4">
      <button onclick="window.print()" class="px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700">
        🖨️ Print
      </button>
      <button onclick="window.close()" class="px-4 py-2 bg-gray-600 text-white rounded hover:bg-gray-700 ml-2">
        ✕ Close
      </button>
    </div>

    <%= yield %>
  </body>
</html>
